{
  "name": "searchbot-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for SearchBot",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "wait-on": "^7.2.0"
  }
}

